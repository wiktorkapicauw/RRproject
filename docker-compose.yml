services:
  app:
    build: .
    volumes:
      - ./output:/app/output
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
    command: quarto render report.qmd --output-dir output
