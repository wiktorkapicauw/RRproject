services:
  app:
    build: .
    volumes:
      - ./output:/app/output
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
    command: sh -c "quarto render report.qmd && cp report.html output/"
